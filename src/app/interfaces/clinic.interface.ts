export interface Clinic {
    id: string;
    name: string;
    address?: string;
    phone?: string;
    email?: string;
    description?: string;
    image?: string;
    comments?: string[];
    // Agrega aquí otras propiedades que necesites
}
